# STA Agent Auto-Triage Bucket File
# Block: d2d1  Run: 26ww14.3  Mode: setup
# Generated: 2026-04-12 19:32:23
#
# Load in Timing Lite: timinglite.py --bucket <this_file> <report>
#
1 PathType:max&&StartPin:^d2d_SECURE_fdfx.*&&EndPin:.*pard2d1misc.*&&PercentPeriod:<-400 CLASSIF_CONSTRAINTS CRITICAL: External INPUT_PATHS from d2d_SECURE inputs to pard2d1misc security/debug latches. Violations >400% clock period (-426% worst). These are input constraint violations - likely missing or incorrect set_input_delay constraints for SECURE fdfx signals (security_policy, debug_capabilities_enabling). Check SDC for proper input delay modeling on SECURE interface. These paths should likely be false paths or multicycle paths if security updates occur asynchronously.
2 PathType:max&&StartPin:^d2d_SECURE_fdfx.*&&EndPin:.*pard2d1chnl.*&&PercentPeriod:<-400 CLASSIF_CONSTRAINTS CRITICAL: External INPUT_PATHS from d2d_SECURE inputs to pard2d1chnl security latches. Similar to bucket 1 - violations >400% clock period. Check input delay constraints on SECURE interface signals to pard2d1chnl. Likely need false path or multicycle path exceptions.
3 PathType:max&&LaunchClk:uclk_mem&&CaptureClk:uclk_mem&&PercentPeriod:<-300 CLASSIF_FCT SEVERE: uclk_mem same-clock paths with violations >300% clock period (-495% worst, 5,757 paths). Split into C2C (5,183 paths, avg -1861ps) and INT (574 paths). The C2C paths suggest chip-to-chip timing budget issues - these cross physical die boundaries with limited optimization headroom. Root cause: structural/floorplan issue. Recommended action: (1) Review uclk_mem C2C budget allocation between dies, (2) Consider pipeline stage insertion at die boundary, (3) Check if any paths can be moved to slower clock domain, (4) Coordinate with physical design for placement optimization at boundaries.
4 PathType:max&&LaunchClk:uclk_mem&&CaptureClk:uclk_mem&&PercentPeriod:>-300&&PercentPeriod:<-100 CLASSIF_PTECO MODERATE: uclk_mem same-clock paths with 100-300% violations (4,935 paths in INT C2C + EXT categories, avg -1323ps to -207ps). These are candidates for automatic PTECO timing ECO: useful skew optimization, cell sizing, buffer insertion. Most paths are cross-partition but with moderate violations that should be closable through datapath optimization.
5 PathType:max&&LaunchClk:uclk_mem&&CaptureClk:uclk_mem&&PercentPeriod:>-100&&PercentPeriod:<-20 CLASSIF_PTECO MINOR: uclk_mem same-clock paths with 20-100% violations (3,220 paths). Good candidates for PTECO auto-fix via cell sizing, useful skew, and buffer insertion. Should close with 1-2 optimization iterations.
6 PathType:max&&LaunchClk:uclk_mem&&CaptureClk:uclk_mem&&PercentPeriod:>-20 CLASSIF_PTECO TRIVIAL: uclk_mem same-clock paths with <20% violations (8,796 paths, avg -12ps for 0-5%, avg -35ps for 5-10%, avg -64ps for 10-15%, avg -85ps for 15-20%). These should close easily with standard optimization - useful skew and minor cell sizing.
7 PathType:max&&PathGroup:OUTPUT_PATHS CLASSIF_CONSTRAINTS CRITICAL: OUTPUT_PATHS with WNS -1793ps (-896% CT), 122 paths, TNS -31358ps. External output timing violations - check set_output_delay constraints. Violations nearly 9x the clock period suggest incorrect output delay modeling or missing multicycle path exceptions. Review output port timing requirements and SDC constraints.
8 PathType:max&&PathGroup:\*\*clock_gating_default\*\* CLASSIF_CONSTRAINTS SEVERE: Clock gating check violations with WNS -1843ps, 20 paths (17 C2C, 3 INT), TNS -24991ps. These are clock gating setup time violations from ucie_phy signals to clock gate enable pins. The violations are extremely large suggesting: (1) Missing set_ideal_latency on parent clock, (2) Incorrect clock gating check constraints, or (3) Path should be a false path if gating signal is asynchronous. Check clock definitions and gating constraints.
9 PathType:max&&LaunchClk:infraclk&&CaptureClk:infraclk CLASSIF_PTECO MODERATE: infraclk same-clock paths with WNS -1623ps, 199 paths (196 C2C, 3 INT). C2C paths avg -1320ps. These cross-partition paths need optimization focus - PTECO useful skew and sizing should help. The INT paths are minor (-1618ps avg for 3 paths).
10 PathType:max&&LaunchClk:uhclk_mem&&CaptureClk:uhclk_mem CLASSIF_PTECO MODERATE: uhclk_mem same-clock C2C paths with WNS -1744ps, 64 paths, avg -1717ps. All are 100-200% violations in C2C category. Cross-partition timing requiring PTECO optimization - useful skew, buffer insertion, and cell sizing.
11 PathType:max&&LaunchClk:croclk&&CaptureClk:croclk CLASSIF_PTECO MODERATE: croclk same-clock C2C paths with WNS -1274ps, 254 paths, TNS -311129ps. All C2C cross-partition paths with avg -1224ps. PTECO optimization target - useful skew and datapath optimization.
12 PathType:max&&PathGroup:FEED_THROUGH CLASSIF_FCT MODERATE: Feed-through paths with WNS -534ps, 8 paths. These are unbuffered or under-buffered feedthrough connections crossing partitions. Requires manual fix - add feedthrough buffers or review if the feedthrough is necessary. May need floorplan adjustment.
13 PathType:max&&LaunchClk:uclk_mem_div8&&CaptureClk:uclk_mem_div8 CLASSIF_PTECO MINOR: uclk_mem_div8 paths with WNS -294ps, 2 paths. Small violation count, should close with basic optimization.
14 PathType:max&&LaunchClk:fscanovrdclk_infraclk&&CaptureClk:fscanovrdclk_infraclk CLASSIF_CONSTRAINTS TRIVIAL: fscanovrdclk_infraclk paths with WNS -147ps, 3 paths. Scan override clock paths - check if these should be excluded from timing analysis during functional mode. May need set_case_analysis on scan enable signals.
